
/*
function UpdatInfo(){
	var nom = document.getElementById('').value;
	var prenom = document.getElementById('').value;
	var numtel = document.getElementById('').value;
	var datenaissance = document.getElementById('').value;
	var nom = document.getElementById('').value;
}

 document.document.getElementById('pourValider').addEventListener("click",validate)
 
function vider(){
	    const nom= document.getElementById('nom');
        const prenom= document.getElementById('prenom');
        const login= document.getElementById('login');
        const numtel= document.getElementById('numtel');

    nom.classList.remove("is-invalid") ;
    nom.classList.remove("is-valid");
    prenom.classList.remove("is-invalid") ;
    prenom.classList.remove("is-valid")

    login.classList.remove("is-invalid") ;
    login.classList.remove("is-valid");
    numtel.classList.remove("is-invalid") ;
    numtel.classList.remove("is-valid")
}
function validate (){
    vider();
    if(nom.value == "") { nom.classList.add("is-invalid") };
    if(prenom.value == "") {prenom.classList.add("is-invalid")}
    if(login.value == "") {login.classList.add("is-invalid")}
    if(numtel.value == "") {numtel.classList.add("is-invalid")}
    nom.value != "" && prenom.value !="" && login.value !="" && numtel.value !="" ? this.send_ajax() : '';
    }

console.log(nom)  */

